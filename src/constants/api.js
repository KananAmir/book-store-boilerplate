const BASE_URL = "http://localhost:8080";

const endpoints = {
  books: "/api/books",
  genres: "/api/genres",
  users: "/api/users",
  orders: "/api/orders",
};

export { endpoints };
export default BASE_URL;
